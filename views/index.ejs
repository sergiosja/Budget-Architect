<!DOCTYPE html>
<html lang="en">
<head>
    <title> YBA </title>
    <link rel="stylesheet" type="text/css" href="/CSS/index.css">
</head>
<body>
    <h1> Your Budget Architect </h1>
    <h4> Welcome back! </h4>

    <!-- If the succesfully signs up -->
    <div id="success">
        <ul>
            <% if (messages.success_msg) { %>
                <li> 
                    <%= messages.success_msg %>
                </li>
            <% } %>
        </ul>
    </div>

    <!-- If the user fails to log in -->
    <div>
        <ul>
            <% if (messages.error) { %>
                <li>
                    <%= messages.error %>
                </li>
            <% } %>
        </ul>
    </div>

    <form id="log" action="/" method="post">
        <div class="box">
            <input type="text" id="username" name="username" placeholder="Username">
            <input type="password" id="password" name="password" placeholder="Password">
            <button type="submit" form="log"> Log in </button>
        </div>
    </form>

    <div class="container"> <a href="/users/signup"> Register new user </a> </div>
</body>
</html>